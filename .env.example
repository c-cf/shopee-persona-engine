PORT=3001
# Optional: enables model-generated copy. Audience discovery remains a labeled demo.
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=openai/gpt-4o-mini

# Generate one audience image after each listing's copy is ready (server only).
# OPENAI_API_KEY=
# OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst
